{
    "repositories": [
      
    ],

    "require": {<% plugins.forEach(function(plugin) { %>
       "<%= plugin.src %>": "^1.0",<% }) %>
       "vlucas/phpdotenv": "2.2"
    },

    "minimum-stability": "dev"
}